# 学习日志：经典机械臂控制与无碰撞操作

## 手眼相机与抓取约束调试

手眼系统的世界坐标转换为：

```text
T_world_camera = T_world_hand · T_hand_camera
p_world = T_world_camera · p_camera
```

本项目采用两阶段感知：固定相机给出粗位置并引导机械臂到观察位姿，腕部相机再进行近距离定位，随后重新求解 IK 和运动规划。seed 7 的三维误差由 11.26 mm 降至 4.26 mm。

原版本在进入 lift 时直接打开刚性 weld。只要夹爪 site 与杯子 site 存在误差，求解器就会在数个仿真步内强制消除误差，视频中表现为瞬移。修正后的原则是：

1. 低速下降，避免跟踪滞后导致手指撞杯沿；
2. 检查左右指尖是否都与杯子接触；
3. 用当前相对位姿定义约束参考系，使初始约束残差为零；
4. 将约束刚度在 0.30 s 内逐渐提高；
5. 把锁定前后杯子位移作为自动化指标。

最终锁定瞬间位移小于 0.31 µm，说明稳定约束没有再移动物体。调试还发现：原随机区域包含靠近中央障碍物的不可抓取点，强制 weld 曾掩盖这个工作空间问题。因此场景随机化也必须满足可达性和末端几何净空，而不能只满足物体位于桌面上。

## RGB-D 感知补充

本阶段把系统从“读取仿真物体坐标”升级为“从相机观测估计坐标”。主要链路为：

```text
RGB 图像 → 杯子像素掩码 → 像素中心 (u, v)
       → 查询深度 z → 相机坐标 → 世界坐标 → IK/规划
```

针孔模型反投影使用：

```text
x_c = (u - c_x) z / f_x
y_c = -(v - c_y) z / f_y
p_w = R_wc [x_c, y_c, -z]^T + t_wc
```

实际调试中，杯口中心是暗色空腔，直接使用蓝色像素中位数会被受光较强的杯沿拉偏；改用连通域包围盒中心后，seed 7 的三维定位误差从约 22 mm 降至 7.14 mm。这说明感知误差不仅来自网络，还会来自材质、光照、遮挡和几何中心定义。

## 本阶段学习目标

通过一个完整 Pick-and-Place 项目复习并串联以下知识：

- 串联机械臂运动学；
- 雅可比矩阵与数值逆运动学；
- 采样式运动规划；
- 时间参数化与轨迹跟踪；
- 机器人动力学补偿；
- MuJoCo 接触、碰撞和约束语义；
- 可复现实验和工程化交付。

## 1. 正运动学与雅可比矩阵

机械臂关节配置记为 `q`，末端位姿为：

```text
x = f(q)
```

在局部线性范围内：

```text
dx = J(q) dq
```

雅可比矩阵同时描述末端线速度和角速度对关节速度的敏感度。七自由度 Panda 对六维末端任务存在一个冗余自由度，因此可以在完成主任务的同时执行关节居中、避限位等零空间目标。

需要掌握：

- 位置雅可比和旋转雅可比的物理意义；
- body Jacobian 与 spatial Jacobian 的区别；
- 奇异值变小时为什么关节速度会放大；
- 冗余机械臂的零空间投影。

## 2. 阻尼最小二乘逆运动学

普通伪逆在奇异位置附近可能数值不稳定。阻尼最小二乘使用：

```text
J# = Jᵀ(JJᵀ + λ²I)⁻¹
dq = J# e
```

阻尼 `λ` 越大，求解越稳定，但收敛精度和速度可能下降。项目中位置误差权重大于旋转误差，并限制每次迭代最大关节步长。

零空间项为：

```text
dq_null = (I - J#J) k(q_center - q)
```

它不会在一阶近似下影响末端任务，但能将关节拉回较舒适的区域。

本次经验：IK 失败不一定意味着算法参数不够好，也可能是“给定姿态下不可达”。应检查最终位置误差、关节是否顶到限位，以及逐步提高目标高度后的可达边界，而不是盲目增加迭代次数。

## 3. 旋转误差

本项目使用坐标轴叉积构造小角度旋转误差：

```text
e_R = 0.5 Σ cross(R_current[:, i], R_target[:, i])
```

这种方法适合迭代式 IK 的局部修正。更系统的方法包括：

- 旋转矩阵 logarithm map；
- 四元数误差；
- SO(3) 李代数中的 `log(R_target R_currentᵀ)`。

后续需要复习 SO(3)/SE(3) 指数映射、对数映射以及 adjoint 变换。

## 4. RRT-Connect

RRT 通过随机采样快速探索高维自由空间。RRT-Connect 同时从起点和终点生长两棵树，每轮尝试将另一棵树连续连接到新节点，通常比单向 RRT 更适合单次查询。

核心步骤：

1. 随机采样关节配置；
2. 搜索树中最近节点；
3. 向采样点移动一个固定步长；
4. 对新边做离散碰撞检测；
5. 另一棵树尝试连续连接；
6. 两树相遇后回溯父节点得到路径；
7. 使用 shortcut 删除不必要的中间节点。

重要认识：只检查路径节点不够，必须对节点之间的边插值检查。如果分辨率过粗，机械臂可能在两个合法节点之间穿过障碍物。

## 5. 配置空间与任务空间

本项目在任务空间定义抓取和放置关键位姿，通过 IK 转为关节空间目标，再使用关节空间 RRT 规划。

这种组合的优点：

- 关键动作容易用末端位置描述；
- RRT 可以显式考虑整条机械臂的碰撞；
- 控制器直接跟踪关节参考，实现简单稳定。

局限：

- IK 解的选择会显著影响规划难度；
- 仅规划若干关键构型，不能自动优化末端路径质量；
- 关节空间捷径可能产生任务空间中较大的摆动。

## 6. 时间参数化

几何路径本身没有速度信息。本项目使用三次 smoothstep：

```text
s(u) = 3u² - 2u³
ds/du = 6u - 6u²
```

其起点和终点速度为零，适合连接离散路径点。但每个 waypoint 都降到零速度会降低效率。更进一步可以学习：

- 五次多项式；
- trapezoidal velocity profile；
- S-curve jerk-limited trajectory；
- TOPP-RA 时间最优路径参数化。

## 7. 计算力矩控制

刚体机械臂动力学为：

```text
M(q)ddq + C(q,dq)dq + g(q) = τ
```

定义参考加速度：

```text
ddq_cmd = Kp(q_ref-q) + Kd(dq_ref-dq)
```

控制输入：

```text
τ = M(q)ddq_cmd + h(q,dq)
```

其中 `h` 包含重力和速度相关项。理想模型下，误差动力学近似变为一组解耦二阶系统。

需要注意：

- 实际力矩必须限幅；
- 轨迹过快会导致持续饱和，从而破坏理论误差动力学；
- `Kd ≈ 2ζ√Kp` 可作为临界阻尼附近的初始设置；
- 模型不准时需要鲁棒项、积分项、扰动观测器或自适应方法。

## 8. MuJoCo 碰撞语义

MuJoCo 的 contact 不等价于已经发生物理穿透。若 geom 设置了正 margin，在距离小于 margin 时就会生成 contact。

因此实验中区分：

- `safety_margin_contact_count`：进入规划安全 margin，但表面距离仍可能为正；
- `forbidden_contact_count`：signed distance 为负的真实禁止碰撞；
- 操作对象与指尖的接触：任务需要的允许接触；
- 机器人与障碍物、桌面或自身的接触：禁止接触。

本次调试中，最初将 margin 内的近接触全部记为碰撞，导致一个实际无穿透的成功轨迹被误判失败。

## 9. 抓取模型与任务抽象

纯摩擦抓取对以下因素十分敏感：

- 接触几何；
- 摩擦系数；
- solver 参数；
- 闭爪力；
- 物体质量；
- 机械臂加速度和姿态变化。

如果研究目标是运动规划和控制，可以在确认闭爪后使用 weld 或 attachment 抽象稳定抓持；如果研究目标是灵巧抓取，则不能使用这种抽象，必须评估接触力、滑移和抓取稳定性。

好的项目报告必须明确说明采用了哪种抽象，以及它排除了哪些问题。

## 10. 本次形成的调试方法

1. 先验证场景能编译和正运动学正确。
2. 单独验证每个关键位姿的 IK，并输出残差。
3. 分别检查 RRT 的起点、终点和路径边是否合法。
4. 先运行无渲染仿真，确认数值指标，再渲染视频。
5. 将“规划路径”和“实际跟踪路径”分开分析。
6. 不只测试一个随机种子。
7. 对失败案例检查物体轨迹，而不是只看最终 success 标志。
8. 区分允许接触、margin 近接触和真实禁止碰撞。

## 面试复习问题

1. DLS IK 相比 Moore-Penrose 伪逆有什么优势？
2. 七自由度机械臂如何利用零空间完成次任务？
3. RRT 为什么概率完备但不渐近最优？
4. RRT-Connect 与 RRT* 的目标分别是什么？
5. 配置空间碰撞检测为什么需要沿边插值？
6. 计算力矩控制对模型误差有多敏感？
7. 重力补偿 PD 与计算力矩控制有什么区别？
8. 如何把几何路径转化为满足速度、加速度和 jerk 限制的轨迹？
9. 仿真中的 contact、penetration 和 margin 分别是什么？
10. 从仿真迁移到真实机械臂时，最先需要替换和标定哪些部分？

## 下一阶段学习清单

- [ ] 系统复习 SO(3)、SE(3)、旋量与李代数。
- [ ] 手工推导二连杆机械臂的 `M(q)`、`C(q,dq)` 和 `g(q)`。
- [ ] 实现普通 PD、重力补偿 PD、计算力矩控制的统一对比。
- [ ] 增加关节力矩、饱和率和能耗指标。
- [ ] 学习 CHOMP、STOMP、TrajOpt 与采样规划的差异。
- [ ] 学习抓取矩阵、force closure 和 friction cone。
- [ ] 将物体位姿来源从仿真真值替换为 RGB-D 视觉估计。
- [ ] 将控制流程封装为 ROS 2 action，并接入 MoveIt 2 做对照实验。

## 11. 载荷感知规划：机器人无碰撞不代表任务无碰撞

抓住物体后，系统的碰撞模型必须从“机器人”扩展为“机器人 + 载荷”。只检查末端点或固定半径球会遗漏三类信息：

- 物体的非球形几何，例如杯把；
- 实际抓取偏心导致的手—物相对位姿误差；
- 控制跟踪误差造成的规划轨迹与执行轨迹差异。

本项目采用的处理方式是：在真实闭爪、双指接触确认和抬升之后测量手—杯变换 `T_hand_object`。对 RRT 的每个候选关节状态，先用正运动学得到 `T_world_hand(q)`，再计算：

```text
T_world_object(q) = T_world_hand(q) T_hand_object
```

随后把完整杯身和杯把放到该位姿，用精确几何距离判断候选状态是否合法。规划安全间隙与执行期最小距离要分别记录：前者用于留出跟踪裕量，后者用于验证真实执行没有穿透。本次 seed 0 的规划名义裕量为 25 mm，但执行期最小距离降到 16 mm，正好说明不能只验证参考路径。

