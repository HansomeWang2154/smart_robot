# 项目日志：MuJoCo 经典控制无碰撞抓取与放置

## 项目信息

- 日期：2026-09-19
- 机器人：Franka Emika Panda
- 仿真器：MuJoCo 3.13
- 任务：从工作台一侧抓取方块，绕过中央障碍物并放置到另一侧目标区
- 项目路径：`/root/shared-nvme/classic_pick_place`
- 运行环境：Ubuntu 24.04、Python 3.10、RTX 4090

## 项目目标

本项目不依赖强化学习策略，构建一条可以解释、复现和量化评估的经典机器人操作链路：

1. 建立包含机械臂、工作台、障碍物、物体和目标区的 MuJoCo 场景。
2. 通过逆运动学求取抓取、抬升、放置等关键位姿对应的关节配置。
3. 使用 RRT-Connect 在七维关节空间内规划无碰撞路径。
4. 使用计算力矩控制器跟踪规划轨迹。
5. 完成抓取、转运、放置和回到初始位姿的状态机。
6. 输出视频、轨迹、碰撞统计和可复现实验指标。

## 实现过程

### 1. 环境与资产

容器中原有 `sim` Conda 环境，包含 MuJoCo、NumPy、SciPy、Matplotlib、ImageIO 和 EGL 渲染支持。根文件系统剩余空间较小，因此代码、机器人模型和实验输出均放在持久化盘 `/root/shared-nvme`。

Franka Panda 模型取自 Google DeepMind MuJoCo Menagerie。场景中增加了：

- 木质工作台；
- 蓝色可抓取方块；
- 绿色圆形目标区；
- 红色中央障碍物；
- 总览与侧面相机；
- 15 mm 障碍物安全检测 margin。

### 2. 逆运动学

使用阻尼最小二乘法求解末端六维位姿误差：

```text
dq = Jᵀ (J Jᵀ + λ²I)⁻¹ e
```

其中位置误差和旋转误差分别计算，旋转误差使用当前坐标轴和目标坐标轴叉积构造。额外加入零空间关节居中项，降低关节靠近限位的概率。

调试中发现，保持夹爪严格竖直向下时，原定 0.705 m 转运高度超出了当前工作区的稳定可达范围。最终将关键转运位姿设为约 0.64 m，并要求规划器从障碍物侧面或更安全的构型绕行。

### 3. 无碰撞规划

实现双向 RRT-Connect：

- 在 Panda 七维关节上下限内随机采样；
- 以固定步长扩展两棵搜索树；
- 对每条候选边进行插值；
- 每个插值点调用 MuJoCo `mj_forward` 和接触检测；
- 对携带物体阶段增加 85 mm 保守载荷包络；
- 规划成功后进行随机捷径平滑。

规划用于三个阶段：

- 初始位姿至预抓取位姿；
- 抬升位姿至预放置位姿；
- 撤离位姿至初始位姿。

### 4. 轨迹与控制

RRT 路径通过三次 smoothstep 插值转为位置和速度参考：

```text
s(u) = 3u² - 2u³
```

底层采用计算力矩控制：

```text
τ = M(q)[Kp(q_ref - q) + Kd(dq_ref - dq)] + h(q, dq)
```

其中：

- `M(q)` 为 MuJoCo 计算的关节空间惯性矩阵；
- `h(q,dq)` 为重力、科里奥利力和离心力组成的 bias force；
- 输出按照 Panda 关节力矩限制裁剪；
- 转运阶段速度限制低于普通移动阶段；
- 每一阶段末端增加短暂保持时间，避免跟踪误差跨阶段积累。

### 5. 抓取建模

第一版完全依赖指尖摩擦。固定随机种子能够成功，但部分 RRT 路径姿态变化较大，方块会从夹爪滑落并停在障碍物顶部。扩大规划间隙和降低速度后，纯接触抓取仍不是所有路径都稳定。

最终实现保留真实闭爪过程，在闭合完成后启用 MuJoCo site weld，放置前解除。该约束用于表示已经形成的稳定刚性抓持，使实验重点落在规划和轨迹控制上。此行为在 README 和 `metrics.json` 中明确记录，没有将其伪装为纯摩擦抓取。

## 关键问题与修复

| 问题 | 原因 | 修复 |
|---|---|---|
| 高位 IK 不收敛 | 竖直夹爪姿态下目标超出可达边界 | 降低转运高度，由规划器绕障 |
| 初始位姿与障碍物相交 | 默认 home pose 的夹爪位于障碍物内部 | 重新求解 pick 侧无碰撞 ready pose |
| MuJoCo 惯性矩阵 API 报错 | 3.13 与旧版本 `mj_fullM` 签名不同 | 同时兼容新旧 API |
| margin 被误记为真实碰撞 | MuJoCo 会提前生成正距离接触 | 仅负 signed distance 计为物理碰撞 |
| 随机路径导致物体滑落 | 纯摩擦抓取对姿态和加速度敏感 | 降速、增加安全壳并启用已披露 weld |
| Git commit 失败 | 容器未配置 Git 用户身份 | 文件已暂存，等待配置用户名和邮箱 |

## 最终结果

使用 RRT 随机种子 `0`、`1`、`2`、`7` 进行四次独立验证，四次全部完成任务。

| 指标 | Seed 7 结果 |
|---|---:|
| 放置 XY 误差 | 5.18 mm |
| 禁止物理碰撞 | 0 |
| 最小机器人—障碍物距离 | 14.73 mm |
| 关节跟踪 RMSE | 0.0242 rad |
| 最大关节跟踪误差 | 0.1290 rad |
| 仿真任务时长 | 28.46 s |
| RRT 转运阶段碰撞检测次数 | 1626 |

单元测试：

- RRT 绕过二维圆形障碍物：通过；
- smoothstep 轨迹端点与端点速度：通过。

## 输出文件

- `outputs/final/pick_place.mp4`：最终演示视频；
- `outputs/final/trajectory.png`：关节误差与物体位置曲线；
- `outputs/final/metrics.json`：实验指标；
- `outputs/final/trajectory.npz`：原始轨迹数据。

## 后续计划

1. 随机化抓取点、放置点、方块质量与摩擦系数，运行 50～100 次 Monte Carlo。
2. 对比关节 PD、重力补偿 PD 和计算力矩控制。
3. 增加轨迹加速度与力矩曲线，评估控制能耗和饱和比例。
4. 将 RRT 替换或扩展为 RRT*、PRM，并比较规划时间和路径长度。
5. 增加 RGB-D 相机，通过视觉估计物体位姿，而不是直接读取仿真真值。
6. 将状态机包装为 ROS 2 action server，为后续真实机械臂迁移做准备。


